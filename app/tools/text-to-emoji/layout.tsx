import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Text to Emoji — Convert, Create & Remove Emoji Text [2026]",
  description:
    "Convert text to emoji, generate emoji letters (🇦–🇿), remove emoji from text, and use shortcodes on Discord, iPhone & Android — plus JS and Python code snippets.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
