import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Emoji Combos — 200+ Copy & Paste Combos for Every Vibe [2026]",
  description:
    "200+ copy-paste emoji combos for Instagram, TikTok, and Discord — cute, funny, aesthetic, and seasonal — plus how to build your own and what popular combos mean.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
